const userController = require('./userController');
const homeController = require('./homeController');
const recipesController = require('./recipesController');

module.exports = {
  userController, homeController, recipesController,
};
