<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cookmaster - Cadastro</title>
  </head>
  <header>
    <h2>Cookmaster</h2>
    <% if (user) { %>
      <p><%= `${user.firstName} ${user.lastName}` %></p>
      <a data-testid="logout" href="/logout">Logout</a>
    <% } else { %>
      <a data-testid="login" href="/login">Login</a>
    <% } %>
  </header>
  <body>
    <h1>Nova Receita</h1>
    <% if (message) {%>
    <div class="message">
      <%= message %>
    </div>
    <% } %>
    <form action="/recipes" method="POST">
      <label for="recipeNameInput">Nome da Receita: </label>
      <input data-testid="nome-receita"
        type="text" name="recipeName" id="recipeNameInput"
      /><br />
      <label for="ingredientsInput">Ingredientes: </label>
      <ul>
        <li></li>
        <button>Excluir Ingrediente</button>
      </ul>
      <input data-testid="ingredientes"
        type="text" name="ingredients" id="ingredientsInput"
      /><br />
      <button data-testid="adicionar-ingrediente" type="button">Adicionar Ingrediente</button>
      <label for="instructionsInput">Modo de Preparo: </label>
      <input data-testid="modo-de-preparo"
        type="text" name="instructions" id="instructionsInput"
      /><br />
      <button data-testid="postar-receita" type="submit">Salvar Receita</button>

      <label for="lastNameInput">Sobrenome: </label>
      <input data-testid="sobrenome" type="text" name="lastName" id="lastNameInput" /><br />
      <button data-testid="login" type="button" onclick="window.location.pathname='/login'">
        Login
      </button>
    </form>
  </body>
</html>
