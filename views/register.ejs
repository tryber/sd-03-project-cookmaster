<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cookmaster - Cadastro</title>
  </head>
  <body>
    <% if(message) { %>
      <h2><%= message %></h2>
    <% } %>
    <form method="POST" action="/register">
      <label for="email"
        >Email
        <input type="email" name="email" data-testid="email" />
      </label>
      <label for="password"
        >Senha
        <input type="password" name="password" data-testid="senha"/>
      </label>
      <label for="confirm-password"
        >Confirmar Senha
        <input type="password" name="confirm_password" data-testid="confirmar-senha"

        />
      </label>
      <label for="name"
        >Nome
        <input type="text" name="name" data-testid="nome"

        />
      </label>
      <label for="lastName"
        >Sobrenome
        <input type="text" name="lastName" data-testid="sobrenome"

        />
      </label>
      <a data-testid="login" href="/login" data-testid="login">Fazer Login</a>
      <button data-testid="cadastrar" type="submit">Cadastrar-se</button>
    </form>
  </body>
</html>
