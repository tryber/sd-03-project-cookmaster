<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cookmaster - Receita</title>
  </head>
  <header>
    <h2>Cookmaster</h2>
    <% if (user) { %>
      <p><%= `${user.firstName} ${user.lastName}` %></p>
      <a data-testid="logout" href="/logout">Logout</a>
    <% } else { %>
      <a data-testid="login" href="/login">Login</a>
    <% } %>
  </header>
  <body>
      <h1><%= recipe.name %></h1>
      <h3>Enviada por <%= recipe.user %></h3>

      <% if (user && user.id === recipe.userId) { %>
        <button data-testid="editar-receita" type="button" onclick="window.location.pathname='/recipes/<%= recipe.id %>/edit'">Editar receita</button>
        <button data-testid="excluir-receita" type="button" onclick="window.location.pathname='/recipes/<%= recipe.id %>/delete'">Excluir receita</button>
      <% } %>

      <h2>Ingredientes</h2>
      <p><%= recipe.ingredients %></p>
      <h2>Modo de Preparo</h2>
      <p><%= recipe.instructions %></p>
      <button type="button" onclick="window.location.pathname='/'">Home</button>
  </body>
</html>
