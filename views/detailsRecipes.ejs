<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
    <%- include('navbar', {user:user}); %>
    <h1>Receitas</h1>
    <div>
      <% if (user) { %>
        <button data-testid="editar-receita" onclick="window.location.pathname='/edit'">Edit</button>
        <button type="button" onclick="window.location.pathname='/recipes/<%= recipesId.id %>/edit'">Editar Receita</button>
        <a href="/recipes/<%= id.id %>/delete">
          <button type="button" data-testid="excluir-receita" >Excluir Receita</button>
        </a>
      <% } %>
      <div>
        <% recipesId.map((elem) => { %>
          <h2>título da receita: <%= elem.name %></h2>
          <h3>ingredientes: <%= elem.ingredients %></h3>
          <p>forma de preparo: <%= elem.instructions %></p>
          <% }) %>
    </div>
  </body>
</html>
