<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cookmaster - Login</title>
  </head>
  <body>
    <% if (message) {%>
    <div class="message"><%= message %></div>
    <% } %>
    <form action="/login" method="POST">
      <label for="passwordInput">Senha: </label>
      <input type="password" name="password" data-testid="senha" id="passwordInput"  /><br />
      <% if (redirect) { %>
      <input type="hidden" name="redirect" value="<%= decodeURIComponent(redirect) %>" />
      <% } %>
      <a href="/register" data-testid="confirmar">Confirmar</a>
      <button type="submit" data-testid="cancelar">Cancelar</button>
    </form>
  </body>
</html>
