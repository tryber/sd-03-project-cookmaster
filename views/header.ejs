<header>
  <nav class="navbar navbar-expand-lg">
      <a class="navbar-brand" href="#"><strong>Cookmaster</strong></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto"></ul>
          <ul class="navbar-nav nav-flex-icons">
              <li class="nav-item">
                <a class="nav-link" href="/admin"><%= user && user.name + ' ' + user.lastName %></a>
              </li>
              <li class="nav-item">
                  <% if (!user) {%>
                  <a data-testid="login" class="nav-link" href="/login">Login</a>
                  <% } else {%>
                  <a data-testid="logout" class="nav-link" href="/logout">Logout</a>
                  <% } %>
              </li>
          </ul>
      </div>
  </nav>
</header>
